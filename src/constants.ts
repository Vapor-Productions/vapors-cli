export const DEFAULT_CONFIG_NAME = 'vapors.config.json';

export const DEFAULT_STRUCTURES = {
  commands: 'src/commands/categories',
  events: 'src/events',
  utils: 'src/utils',
};

export const DEFAULT_REGISTRY_BASE_URL = 'https://registry.zvapor.xyz/registry/';
